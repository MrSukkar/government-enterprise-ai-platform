# Integration Demo Stage 20 runtime contract

Stage 20 extends the localhost-only synthetic demonstration to Governed Enterprise Model Contextualization. It binds the exact accepted Automatic Registration evidence and `AutomaticRegistration`-stopped run to signed OPA scope, an authorized exact Enterprise Object read, structural and relationship revalidation, deterministic result authorization, and append-only PostgreSQL evidence.

Operations must provision all Stage 19 inputs outside Git plus `.runtime/integration-demo-stage-20/enterprise-model/seed.sql`, containing the exact accepted registration receipt, run snapshot, and authorized Enterprise Object. Repository defaults remain empty and fail closed. The demo never substitutes a fake object, read, relationship, authorization, contextualization, or evidence result, and stores no credential, secret, private key, endpoint, trust material, institutional data, or runtime in Git.

The receipt records `EnterpriseModelContextualized: true`, `EnterpriseModelMutated: false`, `WorkflowAdvanced: false`, `EvidenceCompleted: false`, and `CanAdvance: false`. Mutation, traversal, analysis, simulation, inference, action, Evidence completion, and production effects remain prohibited.
