# Integration Demo Stage 12 runtime contract

Stage 12 extends the localhost-only synthetic demonstration to Governed Tests. It connects the exact accepted Sandbox and Security evidence chain to signed action-specific OPA scope, one immutable governed test manifest, an exact institutional test image and supply-chain assurance, the existing provider-neutral `IGovernedTestRuntime` sovereign HTTPS protocol, independent result authorization, and immutable PostgreSQL evidence.

Deployment operations must provision all Stage 11 inputs outside Git plus the exact current sovereign `SandboxImage` record `Platform.Synthetic.Permit.Tests` version `1.0.0` at digest `sha256:4444444444444444444444444444444444444444444444444444444444444444`. They must provision the immutable manifest `manifest://demo/governed-tests/permit-renewal/v1` at digest `5555555555555555555555555555555555555555555555555555555555555555` containing exactly `demo-tests-contract` and `demo-tests-policy-boundary`, then configure `Platform:SoftwareFactory:TestsRuntime` with a real institutionally approved HTTPS endpoint, runtime profile, operator, positive request/response bounds, and pinned response-signing trust. No runtime, image approval, manifest record, trust key, endpoint, private key, credential, or successful test result is stored in Git.

The policy scope is exact: the two required tests and their `Contract` and `Governance` categories, Firecracker-class ephemeral microVM isolation, one demo CPU, 536870912 demo bytes of memory, a 30-second demo timeout, no production credentials, no host filesystem, network default deny with no destinations, and the non-secret `TEST_PROFILE` reference. These are Integration Demo inputs, not institutional SLOs, coverage thresholds, or Production defaults.

Repository defaults remain empty and fail closed. The demo must never substitute a fake runtime, fabricate test discovery or pass results, or report acceptance without a verified signed runtime response. Every required test must be unique, discovered, completed, passed, unskipped, and evidence-bearing. Missing, duplicate, skipped, failed, timed-out, isolation-violating, or unevidenced results fail closed. The receipt records `ProductionEffectOccurred: false` and `CanAdvance: false`; it cannot mutate source, advance to Human Review, reach Production, or grant workflow authority.
