# Operationalization Wave 10 — Governed Security Validation

Wave 10 operationalizes only the approved Increment 11 boundary under CR-011. A verified action-specific OPA permit precedes exact reads of accepted Static evidence, its authoritative inert Code Generation candidate, and a delivery run stopped at `StaticValidation`.

Deployment may configure only an exact set of unique, canonically signed deterministic in-process Security control profiles under pinned RS256 or ES256 trust. The existing `CodeValidationPipeline` executes those controls only at `ValidationGate.Security`. Repository defaults contain no profiles, rules, keys, endpoints, credentials, or institutional data and remain fail closed.

The complete report is independently RBAC/ABAC-authorized and atomically recorded as immutable, idempotent, tenant-scoped PostgreSQL evidence. The receipt is non-executable and non-advancing. No source mutation, compiler, command, process, dynamic acquisition, package operation, network control, external analyzer, AI invocation, candidate or sandbox execution, workflow advancement, Sandbox, or production action is connected.
