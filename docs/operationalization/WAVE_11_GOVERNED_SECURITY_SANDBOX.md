# Operationalization Wave 11 — Governed Security Sandbox

Wave 11 operationalizes only the approved Increment 12 boundary under CR-012. A verified action-specific OPA permit precedes exact reads of accepted Security evidence, its authoritative inert Code Generation candidate, a delivery run stopped at `SecurityValidation`, and the exact institutional Sandbox image.

Deployment must configure the exact provider-neutral sovereign HTTPS runtime endpoint, runtime profile, operator, response-signing trust, request/response bounds, image record and supply-chain trust. OPA must authorize the exact Firecracker-class ephemeral microVM isolation policy, positive CPU/memory/time limits, non-secret environment references, and network-default-deny destination set. Repository defaults configure none of these and remain fail closed.

The runtime response is accepted only when its verified signature binds the request, candidate, image, isolation posture, exact network scope, zero exit, no timeout, no isolation violation, unique output references, and execution evidence. The complete result is independently RBAC/ABAC-authorized and atomically recorded as immutable, idempotent, tenant-scoped PostgreSQL evidence. No production credential, host filesystem, unrestricted networking, workflow advancement, Tests, or production action is connected.
